server {
    listen   80;

    root /usr/share/nginx/html;
    index index.html;

    server_name advtasker-whatsclone.7jlhvw.easypanel.host;

    location / {
        try_files $uri /index.html;
    }
}
